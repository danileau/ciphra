# CIPH-pi20-LB-4 — dev-only Python dependencies for the API.
#
# These install only in the `dev` build target of api/Dockerfile so the
# production image stays minimal. Run tests with:
#
#     docker compose run --rm --build api pytest -q
#
# (Or build the dev target directly: `docker build --target dev ./api`.)

pytest>=8.0,<9.0
pytest-mock>=3.12,<4.0
