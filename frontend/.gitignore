node_modules
.svelte-kit
build
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Playwright artefacts — visual-smoke screenshots are for local
# eyeballing only (Phase B of the deblock plan). Don't commit them.
e2e/_screenshots/
playwright-report/
test-results/
.devcerts/
